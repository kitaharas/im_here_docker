<span id='follow-button_<%= user.id %>' > 
  <% if current_user.following?(user) %>
    <span class="follow-btn unlock">フォロー解除</span>
    
    <%# <%= link_to 'フォロー解除', unfollow_path(user), method: :post, class:'btn btn-info', remote: true %> 
  <% else %>
    <%= link_to 'フォローする', follow_path(user), method: :post, class:'follow-btn', remote: true %>
  <% end %>
</span>

<script>

</script>